# Optional. Unset, the app talks to port 8000 on the same host the page was served from,
# which covers both http://localhost:5173 and http://<your-lan-ip>:5173 without changes.
# Set this only when the API lives somewhere else.
# VITE_API_BASE_URL=http://localhost:8000/api
